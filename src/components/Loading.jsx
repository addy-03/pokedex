import styles from "../styles/Loading.module.css";
const Loading = () => {
  return (
    <div className={styles.wrapper}>
      <div class={styles.pokeball}></div>
    </div>
  );
};

export default Loading;
